#include <stdio.h>
#include <stdlib.h>

double f(int x, int y) {
	if ( (x >= 0) && (x <= 1/3) && (y >= -2/3) && (y <= ))
}

void jacobi(int n, double *grid/*, int kmax*/) {

	double *new_grid;
	//init();
	//for (int k = 0; k < kmax; k++) {
		for (int i = 0; i < n; i++) {
			for (int j = 0; j < 0; j++) {
				grid[(i-1)*n+j]
				grid[(i+1)*n+j]
				grid[i*n+j-1]
				grid[i*n+j+1]
				new_grid[i*n+j] = grid[(i-1]
			}
		}
	//}
}

